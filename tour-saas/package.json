{
  "name": "tour-saas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI tour quoting & itinerary MVP for a Vietnamese travel agency (báo giá + lịch trình tự động)",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
